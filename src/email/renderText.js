function section(title, items) { return `\n## ${title}\n${items.length ? items.map((a, i) => `${i + 1}. ${a.tags.map((t) => `【${t}】`).join('')} ${a.titleZh}（${a.score}/100）\n${a.summaryZh}\n为什么重要：${a.whyImportant}${a.isContinuation ? '\n本次新增进展：与近 7 日记录相比出现新信息。' : ''}\n来源：${a.source}｜${a.publishedAt}\n原文：${a.url}\n交叉来源：${(a.crossSources || []).map((s) => `${s.source} ${s.url}`).join('；') || '暂无'}\n`).join('\n') : '暂无达到质量阈值的条目。\n'}`; }
export function renderText(r) { return `全球时政热点日报｜${r.date}\n过去 24 小时公开新闻摘要\n\n今日一句话总览：${r.overview}\n${section('全球焦点', r.global)}${section('中国重点', r.china)}${section('美国重点', r.usa)}${section('今日观察', r.watch)}\n数据来源与免责声明：GDELT 与公开 RSS；自动摘要仅供信息参考，请核对原始来源。`; }
